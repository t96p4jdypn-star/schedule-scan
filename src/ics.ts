import type{EventDraft}from'./types'
const esc=(s:string)=>s.replace(/\\/g,'\\\\').replace(/,/g,'\\,').replace(/;/g,'\\;').replace(/\n/g,'\\n')
const stamp=(d:string,t:string)=>`${d.replaceAll('-','')}T${t.replace(':','')}00`
export function downloadIcs(e:EventDraft){const a=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Schedule Scan//JA','CALSCALE:GREGORIAN','METHOD:PUBLISH','BEGIN:VEVENT',`UID:${crypto.randomUUID()}@schedule-scan`,`DTSTAMP:${new Date().toISOString().replace(/[-:]/g,'').replace(/\.\d{3}/,'')}`,`SUMMARY:${esc(e.title)}`,`DTSTART:${stamp(e.date,e.startTime)}`];if(e.endTime)a.push(`DTEND:${stamp(e.date,e.endTime)}`);if(e.location||e.address)a.push(`LOCATION:${esc([e.location,e.address].filter(Boolean).join(' '))}`);if(e.notes)a.push(`DESCRIPTION:${esc(e.notes)}`);a.push('END:VEVENT','END:VCALENDAR','');const b=new Blob([a.join('\r\n')],{type:'text/calendar;charset=utf-8'}),link=document.createElement('a');link.href=URL.createObjectURL(b);link.download=`${e.date}_${e.title||'schedule'}.ics`;link.click();URL.revokeObjectURL(link.href)}
